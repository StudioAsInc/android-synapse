<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/md_theme_background"
    android:fitsSystemWindows="true">

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fillViewport="true">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="24dp">

            <!-- Back Button -->
            <ImageButton
                android:id="@+id/btnBack"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:background="@drawable/circle_background"
                android:backgroundTint="@color/pureWhite"
                android:elevation="2dp"
                android:src="@drawable/ic_arrow_back"
                app:tint="@color/auth_dark"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent" />

            <!-- Title -->
            <TextView
                android:id="@+id/tvTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="32dp"
                android:fontFamily="@font/pt_sans"
                android:text="Log in"
                android:textColor="@color/auth_dark"
                android:textSize="32sp"
                android:textStyle="bold"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@id/btnBack" />

            <!-- Subtitle -->
            <TextView
                android:id="@+id/tvSubtitle"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:fontFamily="@font/pt_sans"
                android:text="Enter your email and password to securely access your account and manage your services."
                android:textColor="@color/auth_grey_light"
                android:textSize="14sp"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@id/tvTitle" />

            <!-- Name Input (Sign Up Only) -->
            <EditText
                android:id="@+id/etName"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:layout_marginTop="24dp"
                android:background="@drawable/bg_auth_input"
                android:drawableStart="@drawable/ic_person_enhanced"
                android:drawablePadding="12dp"
                android:drawableTint="@color/auth_grey_dark"
                android:fontFamily="@font/pt_sans"
                android:hint="Name"
                android:paddingStart="20dp"
                android:paddingEnd="20dp"
                android:inputType="textPersonName"
                android:textColor="@color/auth_dark"
                android:textColorHint="@color/auth_grey_light"
                android:visibility="gone"
                app:layout_constraintTop_toBottomOf="@id/tvSubtitle" />

            <!-- Email Input -->
            <EditText
                android:id="@+id/etEmail"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:layout_marginTop="16dp"
                android:background="@drawable/bg_auth_input"
                android:drawableStart="@drawable/ic_email_enhanced"
                android:drawablePadding="12dp"
                android:drawableTint="@color/auth_grey_dark"
                android:fontFamily="@font/pt_sans"
                android:hint="Email address"
                android:paddingStart="20dp"
                android:paddingEnd="20dp"
                android:inputType="textEmailAddress"
                android:textColor="@color/auth_dark"
                android:textColorHint="@color/auth_grey_light"
                app:layout_constraintTop_toBottomOf="@id/etName"
                app:layout_goneMarginTop="24dp" />

            <!-- Password Input Container -->
            <FrameLayout
                android:id="@+id/layoutPassword"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                app:layout_constraintTop_toBottomOf="@id/etEmail">

                <EditText
                    android:id="@+id/etPassword"
                    android:layout_width="match_parent"
                    android:layout_height="56dp"
                    android:background="@drawable/bg_auth_input"
                    android:drawableStart="@drawable/ic_lock_enhanced"
                    android:drawablePadding="12dp"
                    android:drawableTint="@color/auth_grey_dark"
                    android:fontFamily="@font/pt_sans"
                    android:hint="Password"
                    android:paddingStart="20dp"
                    android:paddingEnd="48dp"
                    android:inputType="textPassword"
                    android:textColor="@color/auth_dark"
                    android:textColorHint="@color/auth_grey_light" />

                <ImageButton
                    android:id="@+id/btnTogglePassword"
                    android:layout_width="48dp"
                    android:layout_height="56dp"
                    android:layout_gravity="end|center_vertical"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:src="@drawable/ic_lock_enhanced"
                    app:tint="@color/auth_grey_light"
                    android:padding="12dp" />
            </FrameLayout>

            <!-- Confirm Password Input (Sign Up Only) -->
            <FrameLayout
                android:id="@+id/layoutConfirmPassword"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:visibility="gone"
                app:layout_constraintTop_toBottomOf="@id/layoutPassword">

                <EditText
                    android:id="@+id/etConfirmPassword"
                    android:layout_width="match_parent"
                    android:layout_height="56dp"
                    android:background="@drawable/bg_auth_input"
                    android:drawableStart="@drawable/ic_lock_enhanced"
                    android:drawablePadding="12dp"
                    android:drawableTint="@color/auth_grey_dark"
                    android:fontFamily="@font/pt_sans"
                    android:hint="Confirm Password"
                    android:paddingStart="20dp"
                    android:paddingEnd="48dp"
                    android:inputType="textPassword"
                    android:textColor="@color/auth_dark"
                    android:textColorHint="@color/auth_grey_light" />

                <ImageButton
                    android:id="@+id/btnToggleConfirmPassword"
                    android:layout_width="48dp"
                    android:layout_height="56dp"
                    android:layout_gravity="end|center_vertical"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:src="@drawable/ic_lock_enhanced"
                    app:tint="@color/auth_grey_light"
                    android:padding="12dp" />
            </FrameLayout>

            <!-- Remember Me and Forgot Password -->
            <CheckBox
                android:id="@+id/cbRememberMe"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:text="Remember me"
                android:fontFamily="@font/pt_sans"
                android:textColor="@color/auth_grey_dark"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@id/layoutConfirmPassword" />

            <TextView
                android:id="@+id/tvForgotPassword"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Forgot Password"
                android:fontFamily="@font/pt_sans"
                android:textColor="@color/auth_dark"
                android:textStyle="bold"
                app:layout_constraintBottom_toBottomOf="@id/cbRememberMe"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="@id/cbRememberMe" />

            <!-- Error Message -->
            <TextView
                android:id="@+id/tvError"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:textColor="@color/md_theme_error"
                android:visibility="gone"
                tools:text="Error message"
                app:layout_constraintTop_toBottomOf="@id/cbRememberMe" />

            <!-- Main Button -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnMain"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:layout_marginTop="24dp"
                android:background="@drawable/btn_auth_primary"
                android:fontFamily="@font/pt_sans"
                android:text="Login"
                android:textAllCaps="false"
                android:textSize="18sp"
                android:textStyle="bold"
                app:backgroundTint="@null"
                app:layout_constraintTop_toBottomOf="@id/tvError" />

            <!-- Toggle Mode Text -->
            <LinearLayout
                android:id="@+id/layoutToggle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:orientation="horizontal"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@id/btnMain">

                <TextView
                    android:id="@+id/tvToggleText"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:fontFamily="@font/pt_sans"
                    android:text="Don't have an account? "
                    android:textColor="@color/auth_grey_dark" />

                <TextView
                    android:id="@+id/tvToggleAction"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:fontFamily="@font/pt_sans"
                    android:text="Sign Up here"
                    android:textColor="@color/auth_green"
                    android:textStyle="bold" />
            </LinearLayout>

            <!-- Social Auth Divider -->
            <TextView
                android:id="@+id/tvSocialTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="32dp"
                android:fontFamily="@font/pt_sans"
                android:text="Or Continue With Account"
                android:textColor="@color/auth_grey_light"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@id/layoutToggle" />

            <!-- Social Buttons -->
            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="24dp"
                android:orientation="horizontal"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@id/tvSocialTitle">

                <ImageButton
                    android:id="@+id/btnFacebook"
                    android:layout_width="56dp"
                    android:layout_height="56dp"
                    android:background="@drawable/circle_background"
                    android:backgroundTint="@color/pureWhite"
                    android:elevation="2dp"
                    android:src="@drawable/ic_facebook_logo"
                    android:layout_marginEnd="24dp" />

                <ImageButton
                    android:id="@+id/btnGoogle"
                    android:layout_width="56dp"
                    android:layout_height="56dp"
                    android:background="@drawable/circle_background"
                    android:backgroundTint="@color/pureWhite"
                    android:elevation="2dp"
                    android:src="@drawable/ic_google_logo"
                    android:layout_marginEnd="24dp" />

                <ImageButton
                    android:id="@+id/btnApple"
                    android:layout_width="56dp"
                    android:layout_height="56dp"
                    android:background="@drawable/circle_background"
                    android:backgroundTint="@color/pureWhite"
                    android:elevation="2dp"
                    android:src="@drawable/ic_apple_logo" />
            </LinearLayout>

            <!-- Loading Overlay (reused) -->
             <FrameLayout
                android:id="@+id/loadingOverlay"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="#80000000"
                android:visibility="gone"
                android:elevation="10dp"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent">

                <ProgressBar
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center" />
             </FrameLayout>

        </androidx.constraintlayout.widget.ConstraintLayout>
    </androidx.core.widget.NestedScrollView>
</androidx.constraintlayout.widget.ConstraintLayout>